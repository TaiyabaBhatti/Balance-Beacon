


export function decrypt(metaData){

    
    console.log(metaData)
     
    // function decrypt(grid, cipherText, direction, row, col) {
    //     let original = "";
      
    //     for (let i = 0; i < cipherText.length; i++) {
    //       if (direction === "x") {
    //         // Get center (original) by checking neighbors of cipher letter
    //         const r = row;
    //         const c = col + i;
            
    //         // For example, if cipher was the right neighbor, go left
    //         const left = c - 1 >= 0 ? grid[r][c - 1] : null;
    //         const right = c + 1 < grid.length ? grid[r][c + 1] : null;
      
    //         // Apply your reverse logic here — guess based on neighbors
    //         original += guessOriginalFromAdjacent(cipherText[i], left, right);
    //       }
      
    //       else if (direction === "vertical") {
    //         const r = row + i;
    //         const c = col;
      
    //         const top = r - 1 >= 0 ? grid[r - 1][c] : null;
    //         const bottom = r + 1 < grid.length ? grid[r + 1][c] : null;
      
    //         original += guessOriginalFromAdjacent(cipherText[i], top, bottom);
    //       }
    //     }
      
    //     return original;
    //   }
      







}